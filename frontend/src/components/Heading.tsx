
const Heading = ({ title }: { title: string }) => {
    return (
        <div className="text-green-600 font-extrabold text-5xl mb-10 text-center">
            {title}
        </div>
    )
}

export default Heading
